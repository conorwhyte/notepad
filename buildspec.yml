version: 0.1

phases:
  pre_build:
    commands:
      - echo "STARTING THE BUILD CONOR"
      - npm install
      - npm test

  build:
    commands:
      - npm run build 

artifacts:
  files:
    - ./*
    - src/*
    - dist/*
